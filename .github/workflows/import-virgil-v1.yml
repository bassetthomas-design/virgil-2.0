name: Import Virgil v1 disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Clean rewrite selected
        run: echo "Virgil 2.0 is now a clean rewrite. Import workflow intentionally disabled."
